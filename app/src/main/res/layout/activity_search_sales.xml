<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:padding="16dp">

    <TextView
        android:id="@+id/textView3"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginBottom="12dp"
        android:text="Busca das vendas por período"
        android:textColor="@color/black"
        android:textSize="20sp"
        android:textStyle="normal" />

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Data de Início" />

    <EditText
        android:id="@+id/editDataInicio"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="Digite a data de início (dd/MM/yyyy)"
        android:imeOptions="actionNext"
        android:inputType="text"
        android:minHeight="48dp" />

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Data Final"
        android:paddingTop="16dp" />

    <EditText
        android:id="@+id/editDataFinal"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="Digite a data final (dd/MM/yyyy)"
        android:imeOptions="actionDone"
        android:inputType="text"
        android:minHeight="48dp" />

    <Button
        android:id="@+id/btnBuscarVendas"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="24dp"
        android:background="@color/purple_500"
        android:minHeight="56dip"
        android:text="Buscar Vendas"
        android:textColor="@color/white" />

    <!-- TextView para exibir o valor total das vendas -->
    <TextView
        android:id="@+id/textValorTotal"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Faturamento Total: "
        android:textSize="18sp"
        android:paddingTop="16dp" />

    <!-- TextView para exibir o lucro total ou prejuízo -->
    <TextView
        android:id="@+id/textLucroTotal"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Lucro Total: "
        android:textSize="18sp"
        android:paddingTop="8dp" />

    <!-- RecyclerView para exibir as vendas em formato de card -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/recyclerViewVendas"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="16dp" />

</LinearLayout>